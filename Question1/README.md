# Question 1 – Linux Environment Verification

## whoami
This command displays the currently logged-in username.

## groups
This command shows all Linux groups that my user account belongs to.

## pwd
This command prints the current working directory.

## ls -l
This command lists files and directories in long format showing permissions and ownership.

## echo > user_info.txt
This command creates a file and writes confirmation text into it.

## wc -m user_info.txt
This command counts the number of characters in the file.

## man mkdir
I learned that the -p option creates parent directories if they do not exist.

## ls ~ | sort
This command lists the home directory contents alphabetically.

## grep "admin" log.txt
This command searches and displays lines containing the word admin.

## uname -r
This command displays the Linux kernel version.

## ping
This command tests network connectivity.

## uptime
This command shows system running time, users, and load average.
